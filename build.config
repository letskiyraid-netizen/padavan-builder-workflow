# Основная модель роутера (обязательно!)
MODEL=NEWIFI-D2

# Ветка исходников padavan-ng (develop — самая свежая с фиксами)
BRANCH=develop

# Репозиторий с исходниками (по умолчанию правильный)
FIRMWARE_REPO=https://gitlab.com/hadzhioglu/padavan-ng.git

# Язык веб-интерфейса (ru — русский, en — английский)
FW_LANG=ru

# Включить все модули (AIO = all-in-one): USB, Entware, Transmission, Samba, миниDLNA и т.д.
# Рекомендую включить, но если прошивка получится слишком большой — поставь 0
ENABLE_AIO=1

# Опционально: добавить метку в имя прошивки (чтобы отличать свою сборку)
FW_BUILD=_github_$(date +%Y%m%d)

# Опционально: если хочешь отключить какие-то модули (экономия места)
# DISABLE_MODULES="TRANSMISSION MINIDLNA"

# Опционально: включить/выключить конкретные фичи
# ENABLE_SFTP=1
# ENABLE_PRINTER=1
